name: npm release
# Main pushes and manual runs prepare; only a matching vX.Y.Z tag push publishes.
on:
  push:
    branches: [main]
    tags: ['v[0-9]*']
  workflow_dispatch:
    inputs:
      npm_scope:
        description: 'Preparation-only scope override; defaults to @magicbeansai'
        required: false
        type: string
permissions:
  contents: read
concurrency:
  group: magicvault-npm-release
  cancel-in-progress: false
jobs:
  prepare:
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    outputs:
      scope: ${{ steps.context.outputs.scope }}
      version: ${{ steps.context.outputs.version }}
      publish: ${{ steps.context.outputs.publish }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
      - name: Validate scope, event and matching standalone versions
        id: context
        env:
          NPM_SCOPE: ${{ inputs.npm_scope || '@magicbeansai' }}
        run: node scripts/release-npm.mjs --mode context --scope "$NPM_SCOPE"
      - name: Require the release commit to be on main
        if: steps.context.outputs.publish == 'true'
        run: git merge-base --is-ancestor "$GITHUB_SHA" origin/main
  checks:
    needs: prepare
    uses: ./.github/workflows/ci.yml
  build:
    needs: [prepare, checks]
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: macos-15, platform: darwin-arm64, arch: arm64, target: aarch64-apple-darwin }
          - { os: macos-15-intel, platform: darwin-x64, arch: x64, target: x86_64-apple-darwin }
          - { os: ubuntu-22.04, platform: linux-x64, arch: x64, target: x86_64-unknown-linux-gnu }
          - { os: ubuntu-22.04-arm, platform: linux-arm64, arch: arm64, target: aarch64-unknown-linux-gnu }
          - { os: windows-2022, platform: win32-x64, arch: x64, target: x86_64-pc-windows-msvc }
          - { os: windows-11-arm, platform: win32-arm64, arch: arm64, target: aarch64-pc-windows-msvc }
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    defaults:
      run:
        shell: bash
    env:
      NPM_SCOPE: ${{ needs.prepare.outputs.scope }}
      PLATFORM: ${{ matrix.platform }}
      RUST_TARGET: ${{ matrix.target }}
      CARGO_BUILD_JOBS: '2'
      CARGO_TARGET_DIR: target
      CARGO_INCREMENTAL: '0'
      CARGO_PROFILE_DEV_DEBUG: '0'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          architecture: ${{ matrix.arch }}
      - name: Validate release input and runner
        run: |
          node --input-type=module -e 'const s=process.env.NPM_SCOPE; if (!/^@[a-z0-9][a-z0-9-]{0,63}$/.test(s) || s.trim() !== s || s === "@magicvault-local" || process.platform+"-"+process.arch !== process.env.PLATFORM) process.exit(1);'
      - name: Install Linux build and desktop dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y pkg-config libssl-dev libxkbcommon-dev libxkbcommon-x11-0 libwayland-dev libx11-dev libxrandr-dev libxi-dev libxcursor-dev libgl1-mesa-dev
      - name: Select toolchain
        run: |
          rustup toolchain install 1.92.0 --profile minimal
          rustup override set 1.92.0
          rustup target add "$RUST_TARGET"
      - name: Bundle the Windows C runtime statically
        if: runner.os == 'Windows'
        run: printf '%s\n' 'RUSTFLAGS=-C target-feature=+crt-static' >> "$GITHUB_ENV"
      - name: Source, platform and package checks
        run: |
          python scripts/check_architecture.py
          cargo test --locked --target "$RUST_TARGET" -p magicvault-primitives -p magicvault-prompt -p magicvault-service --lib
          node --test scripts/tests/platform-packages.test.mjs
      - name: Build all four native executables
        run: cargo build --locked --release --target "$RUST_TARGET" -p magicvault -p magicvault-mcp -p magicvault-prompt --features magicvault-prompt/desktop
      - name: Assemble private platform candidate and scope-specific README
        run: |
          mkdir -p dist
          node scripts/package-npm.mjs --binary-dir "target/$RUST_TARGET/release" --output dist/packages --scope "$NPM_SCOPE" --platform "$PLATFORM" --registry-readme
      - name: Execute the actual packaged CLI and MCP
        run: |
          node --input-type=module -e 'import {execFileSync} from "node:child_process"; import fs from "node:fs"; import path from "node:path"; const m=JSON.parse(fs.readFileSync("dist/packages/launcher/package.json")); for(const [n,f] of Object.entries(m.bin)) { if(execFileSync(process.execPath,[path.resolve("dist/packages/launcher",f),"--version"],{encoding:"utf8",timeout:15000}).trim()!==n+" "+m.version) process.exit(1); }'
      - name: Pack exact release tarballs without lifecycle scripts
        run: node scripts/release-npm.mjs --mode pack --packages dist/packages --output dist/tarballs --scope "$NPM_SCOPE" --platform "$PLATFORM"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: MagicVault-npm-${{ matrix.platform }}
          path: dist/tarballs/
          if-no-files-found: error
          retention-days: 14
  verify:
    needs: [prepare, checks, build]
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    env:
      NPM_SCOPE: ${{ needs.prepare.outputs.scope }}
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          pattern: MagicVault-npm-*
          path: dist/release
      - name: Assemble and verify the single universal package
        shell: bash
        run: |
          node scripts/release-npm.mjs --mode assemble --root dist/release --output dist/universal --scope "$NPM_SCOPE" --version "$VERSION"
          node scripts/release-npm.mjs --root dist/universal/tarballs --scope "$NPM_SCOPE" --version "$VERSION" > dist/universal/tarballs/release-plan.json
      - name: Test release integrity, migration and failure handling
        run: node --test scripts/tests/npm-release.test.mjs
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: MagicVault-npm-universal
          path: dist/universal/tarballs/
          if-no-files-found: error
  smoke:
    needs: [prepare, verify]
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: macos-15, arch: arm64 }
          - { os: macos-15-intel, arch: x64 }
          - { os: ubuntu-22.04, arch: x64 }
          - { os: ubuntu-22.04-arm, arch: arm64 }
          - { os: windows-2022, arch: x64 }
          - { os: windows-11-arm, arch: arm64 }
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    env:
      NPM_SCOPE: ${{ needs.prepare.outputs.scope }}
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          architecture: ${{ matrix.arch }}
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: MagicVault-npm-universal
          path: dist/universal
      - name: Install one package offline without optional dependencies and run CLI, MCP and SDK
        shell: bash
        run: node scripts/smoke-npm.mjs --root dist/universal --scope "$NPM_SCOPE" --version "$VERSION" --work dist/smoke
  publish:
    if: needs.prepare.outputs.publish == 'true'
    needs: [prepare, checks, build, verify, smoke]
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
    env:
      NPM_SCOPE: ${{ needs.prepare.outputs.scope }}
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org/'
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: MagicVault-npm-universal
          path: dist/release
      - name: Publish the single main package and verify latest
        shell: bash
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          node scripts/release-npm.mjs --mode publish --root dist/release --scope "$NPM_SCOPE" --version "$VERSION"
      - name: Retire legacy 0.9.0 packages after verified migration
        if: needs.prepare.outputs.version == '0.9.1' && needs.prepare.outputs.scope == '@magicbeansai'
        shell: bash
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: node scripts/release-npm.mjs --mode retire-legacy --scope "$NPM_SCOPE" --version "$VERSION"
  remove-legacy:
    if: needs.prepare.outputs.publish == 'true' && needs.prepare.outputs.version == '0.9.2' && needs.prepare.outputs.scope == '@magicbeansai'
    needs: [prepare, publish]
    runs-on: ubuntu-22.04
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org/'
      - name: Remove only the six deprecated native packages, preserving the main package
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: node scripts/release-npm.mjs --mode remove-legacy --scope @magicbeansai --version 0.9.2
