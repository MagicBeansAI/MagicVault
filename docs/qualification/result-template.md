# Qualification result — YYYY-MM-DD

- Source revision / dirty-worktree description:
- Lockfile revision, compiler and runtime versions:
- OS / architecture / browser version and headed/headless mode:
- Surface and boundary under test:
- Synthetic data and isolation used (no capabilities, raw values or profile paths):
- Commands and elapsed time:

| Case ID | PASS / FAIL / BLOCKED / NOT RUN / NOT IMPLEMENTED | Observation / closed error |
| --- | --- | --- |
| | | |

## Limits and cleanup

State whether consent/key providers were real or synthetic. Distinguish actual
CLI/MCP/native-host subprocesses, real browsers, installed extensions and fixture-
only child processes. Record ignored cases and platform/network constraints.

Confirm which owned browser/server/children were stopped and whether managed
native definitions were installed/removed. Never embed tokens, vault contents,
raw browser/transport dumps, credentials or identifying workstation paths.
